<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:id="@+id/background"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:minWidth="@dimen/swipe_row_background_width"
        android:gravity="center"
        android:orientation="horizontal"
        android:background="@color/white"
        android:weightSum="6"
        app:layout_constraintBottom_toBottomOf="@id/foreground"
        app:layout_constraintEnd_toEndOf="@+id/foreground"
        app:layout_constraintTop_toTopOf="@id/foreground">

        <!--  action button layout will be inflated inside here />-->

    </LinearLayout>

    <LinearLayout

        android:id="@+id/foreground"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!--  layout of the customer will be added inside this wrapper-->

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>